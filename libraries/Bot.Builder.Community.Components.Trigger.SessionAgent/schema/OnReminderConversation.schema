{
  "$schema": "https://schemas.botframework.com/schemas/component/v1.0/component.schema",
  "$role": [ "implements(Microsoft.ITrigger)" ],
  "title": "On Reminder Conversation",
  "description": "Reminder Conversation when user is not interact with in seconds",
  "properties": {
    "ReminderAfterSeconds": {
      "$ref": "schema:#/definitions/integerExpression",
      "title": "Reminder in seconds",
      "description": "Reminder the conversation after seconds",
      "examples": [
        "24"
      ]
    }
  },
  "required": [
  ]
}