{
  "$schema": "https://schemas.botframework.com/schemas/component/v1.0/component.schema",
  "$role": [ "implements(Microsoft.ITrigger)" ],
  "title": "On Expire Conversation",
  "description": "Action is performed when the conversation has expired.",
  "properties": {
    "ExpireAfterSeconds": {
      "$ref": "schema:#/definitions/integerExpression",
      "title": "Expire in seconds",
      "description": "Expire conversation after seconds",
      "examples": [
        "15"
      ]
    }
  },
  "required": [
  ]
}